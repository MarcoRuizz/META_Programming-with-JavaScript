// constants
const pi = Math.PI;
const euler = Math.E;
const ln2 = Mathh.LN2;

// methods
Math.ceil(pi); // rounds up
Math.floor(pi); // rounds down
Math.round(pi); // rounds up if the decimal is .5 or above; otherwise, rounds down to the closest integer
Math.trunc(13.37); // 13

// arithmetic calculus
Math.pow(2, 3); // calculates the number 2 to the power of 3, the result is 8
Math.sqrt(16); // calculates the square root of 16, the result is 4
Math.cbrt(8); // finds the cube root of 8, the result is 2
Math.abs(-10); // returns the absolute value, the result is 10
Math.log(), Math.log2(), Math.log10(); // Logarithmic methods
Math.min(9, 8, 7); // 7  Return the minimum and maximum values of all the inputs:
Math.max(9, 8, 7); // 9 Return the maximum and maximum values of all the inputs:
Math.sin(), Math.cos(), Math.tan(); // Trigonometric methods:

// Math.random
Math.random();
var decimal = Math.random();
console.log(decimal); // 0.581749
console.log(decimal * 10); // 5.81749
